---
import { classMerge } from '../lib/utils'
const props = Astro.props
---

<input
  class={classMerge(
    'mt-1 block w-full rounded-md border-2 border-blue-200 text-primary shadow-sm focus:border-blue-100 focus:ring focus:ring-blue-300 focus:ring-opacity-50',
    props.class
  )}
  {...props}
/>
